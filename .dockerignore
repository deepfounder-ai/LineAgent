# Build artifacts (rebuilt inside the image)
/target

# Local runtime data
/data
*.db
*.db-journal
*.db-wal
*.db-shm

# VCS / CI / editor
.git
.github
.gitignore
.idea
.vscode
*.swp

# Docs that don't affect the build (kept small build context)
docs/
tests/
*.md
*.log
